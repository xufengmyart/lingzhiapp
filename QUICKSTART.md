# 快速启动指南

## 🚀 快速开始

### 前置要求
- Node.js 18+
- Python 3.8+
- Git

### 1. 克隆项目
```bash
git clone https://github.com/xufengmyart/lingzhiapp.git
cd lingzhiapp
```

### 2. 配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量（可选，用于自动化部署）
nano .env
```

### 3. 安装依赖
```bash
# 前端依赖
cd web-app
npm install
```

### 4. 启动开发服务器
```bash
npm run dev
```

访问 http://localhost:5173

## 📦 构建和部署

### 本地构建
```bash
cd web-app
npm run build
```

### 部署到服务器

#### 方式 1: 使用自动化脚本（推荐）
```bash
# 配置 .env 文件中的服务器信息

# 启动自动化监控（会自动检测代码变化并部署）
./auto-deploy.sh start

# 或手动触发一次快速部署
./quick-deploy.sh
```

#### 方式 2: 手动部署
```bash
# 完整部署（包含备份）
./deploy.sh
```

### 启动后台管理
```bash
# 启动后台 API 服务（可选）
./start-admin.sh
```

## 🎯 核心功能

### 用户端功能
1. **中视频项目** (`/medium-video`)
   - 创作 1-30 分钟视频获取收益
   
2. **西安美学侦探** (`/xian-aesthetics`)
   - 探索城市美学，完成任务获得奖励
   
3. **合伙人计划** (`/partner`)
   - 成为合伙人，享受更高收益
   
4. **用户指南** (`/guide`)
   - 了解系统价值，快速开始赚钱

### 管理员功能
1. **后台管理** (`/admin`)
   - 仪表盘查看系统状态
   - 代码编辑功能
   - 部署管理
   - 日志查看
   - 系统监控

## 🔧 常用命令

### 前端开发
```bash
# 启动开发服务器
npm run dev

# 类型检查
npx tsc --noEmit

# 构建
npm run build

# 预览构建结果
npm run preview
```

### 自动化部署
```bash
# 启动监控
./auto-deploy.sh start

# 停止监控
./auto-deploy.sh stop

# 查看状态
./auto-deploy.sh status

# 快速部署
./quick-deploy.sh

# 完整部署
./deploy.sh
```

### 测试
```bash
# 测试所有页面
./test-pages.sh
```

## 📋 检查清单

在部署前，请确保：

- [ ] 已配置 `.env` 文件
- [ ] 已安装所有依赖
- [ ] 本地构建成功
- [ ] 服务器 SSH 连接正常
- [ ] Nginx 配置正确
- [ ] GitHub 推送权限正常

## 🐛 常见问题

### 1. 端口被占用
```bash
# 修改 Vite 配置中的端口号
# web-app/vite.config.ts
server: {
  port: 5174  // 改为其他端口
}
```

### 2. 自动化部署失败
```bash
# 检查日志
tail -f auto-deploy.log

# 检查环境变量
cat .env

# 检查 Git 配置
git config --list
```

### 3. 服务器连接失败
```bash
# 测试 SSH 连接
ssh root@your-server-ip

# 检查防火墙设置
sudo ufw status
```

### 4. 构建失败
```bash
# 清理缓存
rm -rf node_modules package-lock.json
npm install

# 重新构建
npm run build
```

## 📞 获取帮助

- 查看完整文档: `README.md`
- 查看项目总结: `PROJECT-SUMMARY.md`
- 查看配置说明: `CONFIG.md`

## 🎉 开始使用

1. 访问用户指南了解系统价值
2. 选择感兴趣的项目开始参与
3. 完成任务获取灵值奖励
4. 成为合伙人享受更高收益

祝你使用愉快！ 🚀
