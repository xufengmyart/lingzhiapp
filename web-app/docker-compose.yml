version: '3.8'

services:
  app:
    build: .
    container_name: lingzhi-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    networks:
      - lingzhi-network

networks:
  lingzhi-network:
    driver: bridge
