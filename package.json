{
  "name": "lingzhiapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && echo '=== Installing dependencies ===' && npm install && echo '=== Building ===' && npm run build"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.2.0"
  }
}
