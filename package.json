{
  "name": "lingzhiapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && echo '=== Starting build ===' && npm ci --prefer-offline --no-audit --no-fund && echo '=== npm ci completed ===' && npm run build"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.2.0"
  }
}
