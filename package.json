{
  "name": "lingzhiapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && npm install && npm run build"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.2.0"
  }
}
